# 游戏框架状态检查

扫描项目代码并输出当前状态报告，不做任何代码修改。

## 检查内容

1. 扫描 `Assets/_Game/Scripts/` 下每层的所有 .cs 文件
2. 按层分组，列出每层已实现的模块（模块名 + 关键文件数）
3. 对照 `docs/程序/Unity 底层程序架构设计方案 v1.0.md` 标出缺失模块
4. 检查 git status，列出未提交的改动
5. 用表格输出：

```
| 层 | 已实现模块 | 缺失模块 | 完成度 |
```

6. 最后给出"下一步建议"：优先实现哪些模块，理由是什么
